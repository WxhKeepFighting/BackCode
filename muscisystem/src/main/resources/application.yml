spring:
  profiles:
    active: a
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/link01?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT # serverTimezone=GMT这是设置时区
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB
mypro:
  upload_pathname: "F:\\upload\\"
  download_pathname: "F:\\download\\"